default:
    suites:
        default:
            paths:
                - %paths.base%/features/Calculator
                - %paths.base%/features/Tictactoe
                - %paths.base%/features/Biography
            contexts:
                - CalculatorFeatureContext
                - TictactoeFeatureContext
                - BiographyFeatureContext
        calculator:
            paths: [ %paths.base%/features/Calculator ]
            contexts: [ CalculatorFeatureContext ]
        tictactoe:
            paths: [ %paths.base%/features/Tictactoe ]
            contexts: [ TictactoeFeatureContext ]
        biography:
            paths: [ %paths.base%/features/Biography ]
            contexts: [ BiographyFeatureContext ]
